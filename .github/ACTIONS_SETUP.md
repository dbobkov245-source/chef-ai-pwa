66452650# GitHub Actions Setup Guide

Этот проект использует GitHub Actions для автоматизации CI/CD процессов.

## 📋 Созданные Workflows

### 1. **CI** (`.github/workflows/ci.yml`)
Запускается автоматически при каждом push и pull request.

**Что делает:**
- ✅ Проверяет код линтером
- ✅ Собирает проект
- ✅ Тестирует на Node.js 18.x и 20.x
- ✅ Проверяет успешность сборки

**Триггеры:** Push и PR в ветки `main` и `develop`

---

### 2. **PWA Validation** (`.github/workflows/pwa-check.yml`)
Проверяет корректность PWA конфигурации.

**Что делает:**
- ✅ Валидирует `manifest.json`
- ✅ Проверяет наличие обязательных полей
- ✅ Проверяет иконки
- ✅ Проверяет service worker

**Триггеры:** Изменения в `manifest.json`, `sw.js`, `next.config.mjs`

---

### 3. **Deploy to Production** (`.github/workflows/deploy-production.yml`)
Автоматический деплой на Vercel при push в main.

**Что делает:**
- ✅ Запускает тесты
- ✅ Собирает продакшн версию
- ✅ Деплоит на Vercel

**Триггеры:** Push в ветку `main` + ручной запуск

⚠️ **Требует настройки secrets** (см. ниже)

---

### 4. **Version Bump** (`.github/workflows/version-bump.yml`)
Ручное обновление версии проекта.

**Что делает:**
- ✅ Обновляет версию в `package.json`
- ✅ Обновляет версию в `manifest.json`
- ✅ Создает git tag
- ✅ Создает GitHub Release

**Триггеры:** Ручной запуск через Actions UI

---

## 🔐 Необходимые Secrets

Для работы workflows нужно добавить следующие secrets в настройках репозитория:

### Обязательные для всех workflows:
- `GEMINI_API_KEY` - Ваш API ключ от Google Gemini

### Для автоматического деплоя на Vercel:
1. `VERCEL_TOKEN` - Token от Vercel
2. `VERCEL_ORG_ID` - ID вашей организации в Vercel
3. `VERCEL_PROJECT_ID` - ID проекта в Vercel
4. `NEXTAUTH_URL` - URL вашего приложения (production)
5. `NEXTAUTH_SECRET` - Secret для NextAuth

---

## 📝 Как добавить Secrets в GitHub

1. Откройте репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте каждый secret по очереди

---

## 🚀 Как получить Vercel токены

### 1. Получить VERCEL_TOKEN:
```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Получите токен
vercel token
```
Или через UI: https://vercel.com/account/tokens

### 2. Получить VERCEL_ORG_ID и VERCEL_PROJECT_ID:
```bash
# В папке проекта выполните:
vercel link

# После этого откройте .vercel/project.json
cat .vercel/project.json
```

Вы увидите:
```json
{
  "orgId": "team_xxxxxxxxxxxxx",
  "projectId": "prj_xxxxxxxxxxxxx"
}
```

---

## 🎯 Как использовать workflows

### Автоматические workflows (ничего делать не нужно):
- **CI** - запускается автоматически при push/PR
- **PWA Check** - запускается при изменении PWA файлов
- **Deploy** - запускается при push в `main`

### Ручные workflows:

#### Обновить версию:
1. Откройте **Actions** в GitHub
2. Выберите **Version Bump**
3. Нажмите **Run workflow**
4. Выберите тип обновления:
   - `patch` - 0.1.0 → 0.1.1 (исправления)
   - `minor` - 0.1.0 → 0.2.0 (новые функции)
   - `major` - 0.1.0 → 1.0.0 (breaking changes)

---

## ✅ Проверка работы

После добавления secrets и push кода:

1. Перейдите в **Actions** на GitHub
2. Вы должны увидеть запущенные workflows
3. Проверьте, что все галочки зеленые ✅

---

## 🔧 Отключение автодеплоя

Если не хотите автоматический деплой на Vercel, можно:

1. Удалить файл `.github/workflows/deploy-production.yml`
2. Или закомментировать `on: push:` секцию, оставив только `workflow_dispatch:`

---

## 📚 Дополнительная информация

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Vercel CLI Documentation](https://vercel.com/docs/cli)
- [Vercel GitHub Integration](https://vercel.com/docs/deployments/git/vercel-for-github)

---

**Создано:** 2025-11-30  
**Версия:** 1.0.0
